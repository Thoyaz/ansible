- name: Install Tools
  hosts: localhost
  connection: local
  vars:
    tools:
    - nginx
  
  tasks:
  - name: install nginx
    ansible.builtin.package:
      name: "{{ item }}"
      state: present
    loop: "{{ tools }}"
