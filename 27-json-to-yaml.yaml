# ==== NOT WORKING ====

- name: Read JSON using slurp and convert to variable
  hosts: local
  connection: local
  tasks:
    - name: read ubuntu yaml
      ansible.builtin.set_fact: # setting the variable
        student_yaml: "{{ lookup('file', 'ubuntu-facts.yaml' ) | from_yaml }}"

    - name: convert YAML to JSON
      ansible.builtin.debug:
        msg: "{{ student_yaml | to_nice_json  }}"