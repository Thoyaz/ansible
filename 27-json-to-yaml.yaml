- name: Convert JSON to XML
  hosts: localhost
  connection: local
  vars:
    my_data: "{{ lookup('ansible.builtin.file', 'ubuntu-facts.json') | from_json }}"

  tasks:
    - name: convert ubuntu facts json file to xml
      debug:
        msg: "{{ my_data }}"
