- name: Read JSON using slurp and convert to variable
  hosts: localhost
  connection: local
  gather_facts: false

  tasks:
    - name: Find out what the remote machine's mounts are
      ansible.builtin.debug:
        msg: "the value of foo.txt is {{ lookup('ansible.builtin.file', 'C:\\Users\\thugs\\Desktop\\TZ\\88S\\Ansible\\ubuntu-facts,json') }}"

    - name: display multiple file contents
      ansible.builtin.debug: var=item
      with_file:
        - "C:\\Users\\thugs\\Desktop\\TZ\\88S\\Ansible\\ubuntu-facts,json"